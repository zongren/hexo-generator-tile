<?xml version="1.0" encoding="utf-8"?>
<tile>
  <visual>
    <binding template="TileSmall">
      {{ post.title }}
    </binding>
    <binding template="TileMedium">
      {{ post.title }}
    </binding>

    <binding template="TileWide">
      <text hint-style="subtitle">{{ post.title }}</text>
      <text hint-style="captionSubtle">
      {% if post.description %}
      {{ post.description }}
      {% elif post.excerpt %}
      {{ post.excerpt }}
      {% elif post.content %}
      {{ post.content.substring(0, 20) }}
      {% endif %}
      </text>
    </binding>

    <binding template="TileLarge">
     <text hint-style="subtitle">{{ post.title }}</text>
      <text hint-style="captionSubtle">
      {% if post.description %}
      {{ post.description }}
      {% elif post.excerpt %}
      {{ post.excerpt }}
      {% elif post.content %}
      {{ post.content.substring(0, 20) }}
      {% endif %}
      </text>
    </binding>

  </visual>
</tile>